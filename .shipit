steps = FindVersion, ChangeVersion, CheckChangeLog, Manifest, DistTestExtra, Commit, Tag, MakeDist, MyUploadCPAN, DistClean, Twitter

git.tagpattern = Text-Pipe-%v
git.push_to    = origin

# svn.tagpattern = http://svn.coderepos.org/share/lang/perl/Text-Pipe/tags/Text-Pipe-%v

twitter.config = ~/.twitterrc
twitter.distname = Text-Pipe
twitter.message = shipped %d %v - soon at %u
